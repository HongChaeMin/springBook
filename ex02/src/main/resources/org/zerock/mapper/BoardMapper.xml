<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.zerock.mapper.BoardMapper">

	<select id="getList" resultType="org.zerock.domain.BoardVO">
		<![CDATA[
			SELECT
				BON
				, TITLE
				, WRITER
				, REGDATE
				, UPDATEDATE
			FROM
				TBL_BOARD
			WHERE 
				BON > 0
		]]>
	</select>
	
	<insert id="insert">
		INSERT INTO TBL_BOARD (
			BON
			, TITLE
			, CONTENT
			, WRITER
		) VALUES (
			SEQ_BOARD.NEXTVAL
			, #{title}
			, #{content}
			, #{writer}
		)
	</insert>
	
	<insert id="inserSelectKey">
		<selectKey keyProperty="bon" order="BEFORE" resultType="long">
			SELECT
				SEQ_BOARD.NEXTVAL FROM DUAL
		</selectKey>
		INSERT INTO TBL_BOARD (
			BON
			, TITLE
			, CONTENT
			, WRITER
		) VALUES (
			#{bon}
			, #{title}
			, #{content}
			, #{writer}
		)
	</insert>
	
	<select id="selectBorad" resultType="org.zerock.domain.BoardVO">
		SELECT
			BON
			, TITLE
			, WRITER
			, REGDATE
			, UPDATEDATE
		FROM
			TBL_BOARD
		WHERE 
			BON = #{bon}
	</select>
	
	<delete id="deleteBoard">
		DELETE FROM 
			TBL_BOARD
		WHERE 
			BON = #{bon}
	</delete>
	
	<update id="updateBoard">
		UPDATE TBL_BOARD
		SET
			TITLE = #{title}
			, CONTENT = #{content}
			, WRITER = #{writer}
			, UPDATEDATE = sysdate
		WHERE
			BON = #{bon}
		
	</update>
	
</mapper>